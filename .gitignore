# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# full icon pack dumped at the repo root for picking from; only the
# curated set actually used gets copied into public/icons and committed
/png

# original full-size favicon source dropped at the repo root; the
# resized versions actually used live in public/ and are committed
/favicon.png

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
